# Aplicación
SECRET_KEY=cambia_esto_por_una_clave_segura_de_32_chars
DATABASE_URL=sqlite:///./school.db

# CORS — URL del frontend
FRONTEND_URL=http://localhost:5173

# Encriptación de campos sensibles (API keys)
ENCRYPTION_KEY=genera_con_python_-c_"from_cryptography.fernet_import_Fernet;print(Fernet.generate_key().decode())"
